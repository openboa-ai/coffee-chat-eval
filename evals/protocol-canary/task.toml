schema_version = "1.4"
artifacts = ["/app/protocol-canary.json"]

[task]
name = "openboa-ai/protocol-canary"
version = "2026.8.12"
description = "Evaluator protocol canary; not a Coffee Chat performance benchmark."
keywords = ["coffee-chat", "protocol-canary"]

[[task.authors]]
name = "OpenBoa AI"

[metadata]
evidence_class = "implementation"
measurement = "unmeasured"

[verifier]
timeout_sec = 30.0
environment_mode = "separate"
collect = []

[verifier.env]

[agent]
timeout_sec = 120.0

[environment]
network_mode = "no-network"
build_timeout_sec = 300.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
