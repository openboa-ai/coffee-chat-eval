{
  "name": "coffee-chat-eval",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Clean-context evaluation runner and evidence store for Coffee Chat Product and Judge iterations.",
  "scripts": {
    "hooks:install": "git config core.hooksPath .githooks",
    "verify": "node --test .github/verify.test.mjs && node .github/verify.mjs"
  }
}
